apiVersion: v1
kind: Service
metadata:
  name: circleguard-promotion-service
  labels:
    app: circleguard-promotion-service
spec:
  selector:
    app: circleguard-promotion-service
  ports:
    - port: 8088
      targetPort: 8088
      name: http
  type: ClusterIP
