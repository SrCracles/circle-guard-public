apiVersion: v1
kind: Service
metadata:
  name: circleguard-identity-service
  labels:
    app: circleguard-identity-service
spec:
  selector:
    app: circleguard-identity-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
  type: ClusterIP
