apiVersion: v1
kind: Service
metadata:
  name: circleguard-auth-service
  labels:
    app: circleguard-auth-service
spec:
  selector:
    app: circleguard-auth-service
  ports:
    - port: 8180
      targetPort: 8180
      name: http
  type: ClusterIP
