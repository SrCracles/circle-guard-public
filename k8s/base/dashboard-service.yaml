apiVersion: v1
kind: Service
metadata:
  name: circleguard-dashboard-service
  labels:
    app: circleguard-dashboard-service
spec:
  selector:
    app: circleguard-dashboard-service
  ports:
    - port: 8084
      targetPort: 8084
      name: http
  type: ClusterIP
